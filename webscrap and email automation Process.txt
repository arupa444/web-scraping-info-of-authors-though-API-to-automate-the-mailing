any loaction:
	->press windows + r
	->type "CMD"
	->cd Desktop
	->cd finalStore


To send mail, filter mail and to scrap mail:

	->cd web-scraping-info-of-authors-though-API-to-automate-the-mailing-main
	->uvicorn app:app --reload
	#if that won't work then try changing the port
	->uvicorn app:app --reload --port 8001

	note: you can use port inbetween 8000 to 8005
